# Convert historical workflow binary judge rubric

For each selected workflow step, return resolved only when the retained candidate evidence is harness-valid, bound to the planned exact model and scoring identity, completes the step's stated outcome, and cites sufficient redacted evidence. Otherwise return unresolved with a concise reason. Candidate identity remains blinded; candidate execution is never repeated during rejudge.
